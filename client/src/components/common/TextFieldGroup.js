import React from "react";

const TextFieldGroup = ({
  mame,
  placeholder,
  value,
  label,
  error,
  info,
  type,
  onChange,
  disabled
}) => {
  return <div />;
};

export default TextFieldGroup;
